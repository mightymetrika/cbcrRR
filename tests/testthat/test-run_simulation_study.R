# test_that("multiplication works", {
#   sim_res <- run_simulation_study(n_reps = 2, covar_noise = list(c(0, 1)),
#                                   beta_slo_covar=c(0.1))
#   expect_equal(2 * 2, 4)
# })
